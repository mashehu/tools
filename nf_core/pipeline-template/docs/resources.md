# {{ name }}: Resources

This page summarises the default compute resources (CPUs, memory and time) that each
module in the pipeline requests. It is useful for getting a rough idea of what a pipeline
run will need before launching it.

The table below is **auto-generated** by `nf-core pipelines lint --fix resources_table` and
kept up to date by the pipeline linting. Do not edit the block between the markers by hand —
adjust the resources in `conf/base.config` (labels / defaults) or `conf/modules.config`
(`withName` overrides) and regenerate.

<!-- nf-core resources table start -->
<!-- nf-core resources table end -->
